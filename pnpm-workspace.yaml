packages:
  - apps/*
  - packages/*

# esbuild / workerd 的构建脚本需要显式批准（pnpm v10+ 默认不执行依赖构建脚本）。
# pnpm 11 的正式键是 allowBuilds（map 形式）；onlyBuiltDependencies 仅为兼容仍被识别，二选一即可。
allowBuilds:
  esbuild: true
  workerd: true
